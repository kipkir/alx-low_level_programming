#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0
 */

int main(void)

{
	char alphabet;

	for (alphabet = "a"; alphabet <= "zi"; alphabet++)
{
	putchar(alphabet);
}

putchar("\n");

return (0);
}
